//Parte-A
const cuadrado =(n) => n * n;

const sumar = (a, b) => a + b;

const inicial=(nombre)=> nombre[0];

//Parte-B
const mitad = (n) => n / 2; //
const triple = (n) =>  n * 3; ; // Esta funcion no devuelve nada porque esta mal la sintaxis, para que devuekva algo hay que quitar las llaves.

//parte-C
const precioConIva = (precio) =>  precio * 1.21 
console.log(precioConIva(100))